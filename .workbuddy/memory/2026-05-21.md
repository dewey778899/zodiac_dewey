# 2026-05-21 工作记录

## 项目启动 & GitHub 推送

- 项目：zodiac_dewey（小登哥的灵魂合盘 H5 应用）
- 技术栈：Spring Boot 3.2 / Java 17 + H2 / MySQL + DeepSeek AI
- **构建**：`mvn clean package -DskipTests` 成功，生成 `backend/target/zodiac-dewey.jar`
- **启动**：加载 `.env` 后以后台进程方式启动后端，健康检查 `http://localhost:8080/api/health` 返回正常
- **推送**：`git push origin main` 推送至 `https://github.com/dewey778899/zodiac_dewey`，提交 `f63c75e`

## Docker 镜像 & 部署

- 镜像地址：`dewey778899/zodiac-dewey:latest`
- Docker Hub：https://hub.docker.com/r/dewey778899/zodiac-dewey
- Dockerfile、docker-compose.yml 已创建，GitHub Actions 自动 CI 构建（.github/workflows/docker-build.yml）
- 需要在 GitHub Secrets 中配置 `DOCKERHUB_USERNAME` 和 `DOCKERHUB_TOKEN` 后，Actions 才能推送镜像
- Actions 每次 push main 分支自动构建，提交 `774af26`

## 双模型切换 + 支付弹窗功能

- 功能：前端模型切换栏（DeepSeek免费 / Claude¥9.9）+ 支付弹窗（微信/支付宝二维码切换）
- 后端：AiChatService 支持 DeepSeek 和 Claude 双模型，不同的 URL、Auth Header、请求/响应格式
- Claude API Key 通过环境变量 `CLAUDE_API_KEY` 注入，yml 中不写明文
- 支付二维码图片：`frontend/img/wechat_qr.jpg`、`frontend/img/alipay_qr.jpg`
- localStorage 持久化：`zodiac_model`（模型选择）、`zodiac_claude_paid`（支付状态）
- 支付验证第一阶段为纯前端手动确认，无后端校验
- 提交 `7595d85`，已推送到 GitHub

## 手机访问 & HTTPS 隧道

- 手机无法访问 HTTP 页面（iOS Safari 限制混合内容）
- 创建了 Node.js 反向代理（端口 3000），合并前后端到单端口，消除 CORS
- 使用 cloudflared 建立 HTTPS 隧道：`cloudflared tunnel --protocol http2 --url http://localhost:3000`
- cloudflared 需用 `--protocol http2`，默认 QUIC 在当前网络环境下会超时
- 前端 API_BASE 新增代理端口 3000 同域逻辑和局域网 IP 路由，提交 `47a5c8d`，已推送 GitHub

## Docker Hub 迁移

- 镜像地址从 `dewey778899/zodiac-dewey` 迁移到 `dwaigx/zodiac-dewey`
- Docker Hub：https://hub.docker.com/r/dwaigx/zodiac-dewey
- 需要在 GitHub Secrets 配置 `DOCKERHUB_USERNAME=dwaigx` 和 `DOCKERHUB_TOKEN`
- 合并了远程的 Claude 环境变量（docker-compose.yml 新增 CLAUDE_* 变量）和 ghcr 改进
- 提交 `816b40d`，已推送到 GitHub

## Docker 镜像端到端验证 ✅

- 从 Docker Hub 拉取 `dwaigx/zodiac-dewey:latest` 成功
- `docker run -d --name zodiac_dewey -p 8080:8080 -e AI_API_KEY=xxx -v zodiac_data:/app/data dwaigx/zodiac-dewey:latest`
- Spring Boot 6.7 秒启动，零错误零重启
- 前端页面 200 OK（85KB HTML），10/10 功能检查全部通过
- DeepSeek API 返回完整合盘报告（4278 chars），HTTP 200
- 支付二维码图片正常加载（wechat_qr 68KB, alipay_qr 198KB）
- **结论：镜像功能完全正常，Docker Hub 部署流程验证通过**

## Claude 验证 & 代理配置

- 原模型名 `claude-sonnet-4-20250514` 已被 Anthropic 弃用（返回 404），已改为 `claude-sonnet-4-5-20250929`
- 用户指定使用 `claude-opus-4-7` 模型
- **Docker 容器内无法直接访问 Anthropic API**（中国大陆网络限制）
- Reactor Netty WebClient 不遵守 JVM 系统属性代理，必须在代码中通过 `HttpClient.proxy()` 配置
- 新增 `AI_PROXY_HOST` / `AI_PROXY_PORT` 环境变量，AiChatService 的 DeepSeek 和 Claude WebClient 均支持代理
- 本地代理 `127.0.0.1:10809` 只监听 localhost，Docker 容器需通过端口转发或 Node.js 代理中转
- **宿主机直接运行后端 Claude 完美工作**（2.3 秒返回报告），Docker 容器内代理仍有问题待解决
- 提交 `a639fdb`（代理+模型修复）、`5f4c2c0`（.dockerignore），已推送 GitHub
- 本地 Docker 构建上下文打包报错（tar: missed writing bytes），依赖 GitHub Actions CI 构建推送镜像

## 注意事项

- `.env` 已配置 DeepSeek API Key，本地 H2 数据库，8080 端口
- 前端访问：`cd frontend && python -m http.server 5173` → `http://localhost:5173`
- 代理访问：`node /tmp/zodiac_proxy.js` → `http://localhost:3000`（前后端合一）
- 手机测试：`cloudflared tunnel --protocol http2 --url http://localhost:3000`，获取 HTTPS 链接扫码访问
- 生产部署另见 `DEPLOYMENT_README.md`
- 本地启动后端需设置环境变量：`SERVER_PORT=8080 CLAUDE_API_KEY=xxx mvn spring-boot:run`
- 环境变量 `SERVER__PORT=51770` 会覆盖 Spring Boot 端口配置，需显式指定 `SERVER_PORT=8080`
- **Docker 部署 Claude 注意**：容器需配置 `AI_PROXY_HOST` + `AI_PROXY_PORT` 指向可达的 HTTP 代理
- Node.js CONNECT 代理转发器：`/c/Users/user/Desktop/http_proxy.js`，监听 `0.0.0.0:10810` 转发到 `127.0.0.1:10809`

## 前后端分离双镜像部署

- 拆分为两个独立 Docker 镜像：
  - `dwaigx/zodiac-dewey-backend:latest` — Spring Boot 后端（Java 17 JRE Alpine）
  - `dwaigx/zodiac-dewey-frontend:latest` — Nginx 前端（静态文件 + /api/ 反向代理）
- 新增 `backend/Dockerfile` 和 `frontend/Dockerfile`
- 前端 Nginx 配置：`/api/` 自动代理到 `http://backend:8080/api/`
- docker-compose.yml 改为双服务编排，前端暴露 80 端口，后端不暴露端口（仅内部通信）
- GitHub Actions CI 更新为构建推送双镜像
- README 重写为最简部署文档
- 提交 `aa31a10`，已推送 GitHub，CI 自动构建中

## 默认 Claude + 价值说明弹窗（2026-05-22）

- 默认模型改为 `claude`（深度解析），用户首次打开页面默认在付费 tab
- 新增价值说明弹窗（`value-modal`），每个设备只弹一次（`zodiac_value_modal_seen` localStorage 标记）
- 弹窗展示付费版 5 大优势：报告深度翻倍、五大星盘维度、真人占星师体验、未来运势预演、6条珍藏锦囊
- 弹窗可关闭，关闭后留在当前页；用户可选择"先用免费版"切换到免费 tab
- 从免费版切换到深度解析时，先显示价值弹窗而非直接弹出支付二维码
- 模型切换栏文案："DeepSeek·免费"改为"免费版·基础解析"
- 提交 `0f3f957`，已推送 GitHub，CI 自动构建双镜像中
